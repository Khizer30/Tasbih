generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../src/db/store.db3"
}

model Counter {
  name  String @id @unique @default("")
  value Int    @default(0)
}
